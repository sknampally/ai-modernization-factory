# Sample JavaScript app

Tiny Node.js sample used by AIMF for onboarding and automated tests.

Point `aimf.toml` at this directory (default) or assess it directly:

```bash
aimf assess --repo examples/sample-js-app --output reports
```
