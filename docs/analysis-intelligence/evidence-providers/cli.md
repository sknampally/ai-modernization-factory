# CLI

```bash
aimf evidence providers list
aimf evidence providers inspect language.python.core
aimf evidence providers explain language.python.core --repository .
aimf evidence capabilities
aimf evidence plan --repository .
```

Filters: `--language`, `--capability`, `--json`.

CLI remains `aimf` (no codestrata rename in this milestone).
