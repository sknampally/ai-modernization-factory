# Artifacts

`architecture-assessment.json` is written under the assessment run directory.

Deterministic JSON (`sort_keys`, stable indent). Atomic write via temp + replace.
Does not contain raw source content.
