# Finding Relationships

Deterministic catalog among architecture rules. No text similarity.

Examples:

- cycle + invalid direction → reinforces (same boundary)
- coupling + cycle → supports only with shared unit (not clustered into boundary)
- direction + layer boundary → overlaps
- framework leakage + layer boundary → reinforces

Unrelated findings remain independent.
