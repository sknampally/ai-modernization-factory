# Clustering

Union-find over related findings. Coupling stays separate from boundary
integrity (Option A) unless a future policy explicitly documents same-root-cause
clustering.

Primary finding selection uses severity, rule precedence, confidence, then
stable finding ID.
